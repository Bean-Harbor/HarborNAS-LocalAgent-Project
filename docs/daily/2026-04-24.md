# 2026-04-24 Harbor Daily Sync

## Lane Sync - harbor-architect - 2026-04-24

### Repo / Branch

- Repo: `HarborBeacon`
- Branch: `codex/phase-d-sync-2026-04-16`
- PR: [#3](https://github.com/Bean-Harbor/HarborBeacon/pull/3)

### Today's Scope

- Execute the approved `.182`-first Candle cutover rehearsal without widening any frozen northbound or IM seams.
- Keep `4176 -> openai_proxy` as the live default before and after the rehearsal.
- Run framework and IM regressions in parallel and keep AIoT device ownership separate from HarborOS system control.

### Current Runtime Baseline

- `4176` remains the live HarborOS default backend on `openai_proxy`.
- `4186` remains the Candle candidate side lane on HarborOS `.182`.
- HarborOS target-runtime Candle report is now promotable:
  - `/mnt/software/harborbeacon-agent-ci/release-evidence/local-model-benchmark-candle-qwen3-jina-harboros-spawned.json`
  - `gate.promotable=true`
- Builder `.197` is now the active release builder; `.223` is history-only for the earlier mirror / prefetch lane.
- HarborOS `.182` remains the only runtime gate that matters for Candle cutover readiness.

### Completed

- Local checks:
  - `cargo test --bin harbor-model-api --quiet`
  - `python -m pytest tests/contracts/test_harbor_integration.py tests/contracts/test_run_scripts_regression.py -q`
  - `python -m pytest tests/contracts/test_contract_docs.py tests/contracts/test_aiot_boundary_guard.py -q`
- HarborGate local seam regressions:
  - `python -m pytest tests/test_gateway.py tests/test_harborbeacon.py tests/test_platform_registry.py tests/test_weixin_adapter.py -q`
  - Result: `60 passed`
- HarborOS `.182` pre-switch task-path matrix stayed green on the live `4175/4176` path:
  - capability summary
  - friendly unsupported
  - explicit retrieval with citations
  - natural-language retrieval with citations
  - explicit and natural-language retrieval require `roots=/mnt/software/harborbeacon-agent-ci/acceptance-corpus` for today's repeatable acceptance corpus
- AIoT live device-domain guard on `.231` stayed green:
  - HarborOS `.182` runtime media-tools `ffprobe` at `/var/lib/harborbeacon-agent-ci/runtime/media-tools/bin/ffprobe` still reached `rtsp://admin@192.168.3.231/stream1` with `codec=hevc` and `resolution=2880x1620`
  - the same probe still reached `/stream2` with `codec=h264` and `resolution=640x480`
  - HarborOS snapshot proxy `http://192.168.3.182:4174/api/cameras/cam-rtsp-192-168-3-231/snapshot.jpg` still returned `HTTP 200` with `image/jpeg`
  - this keeps `snapshot/share_link/inspect/control/discover` inside the AIoT device domain rather than drifting into HarborOS system ownership
- HarborGate redacted gateway status remained reachable on `.182` through `GET /api/gateway/status` with the frozen `X-Contract-Version: 1.5` and service auth.
  - Current live IM blocker is still external/platform-side, not HarborBeacon seam drift:
    - Weixin `transport.status=error`
    - `last_error=<urlopen error [Errno -2] Name or service not known>`
- Release packaging lane was advanced to a formal runtime-wired Candle bundle on builder `.197`:
  - current local HarborBeacon and HarborGate working trees were synced to `/home/harbor-innovations/src/HarborBeacon` and `/home/harbor-innovations/src/HarborGate`
  - `x86_64-unknown-linux-musl` packaging initially failed on `openssl-sys` because the new local-model dependency graph still pulled `reqwest system-proxy` and `hf-hub` default native-tls features into the release build
  - fixed the portability gap by tightening Cargo features to:
    - remove `system-proxy` from the top-level `reqwest` dependency
    - switch `hf-hub` to `default-features = false` with only the sync `ureq` path
  - local verification after the fix stayed green:
    - `cargo test --bin harbor-model-api --quiet`
    - `python -m pytest tests/contracts/test_release_packaging_install_lane.py -q`
  - rebuilt formal release bundle on builder:
    - version: `harbor-release-20260424-candle-runtime-r2`
    - HarborBeacon git ref: `snapshot`
    - HarborGate git ref: `snapshot`
    - tarball sha256: `d0093ebae58d4d19b69e808675ccb600bead4cca0b5cb2b21b2e0b9d757afede`
    - packaged `bin/harbor-model-api` is `statically linked`
    - packaged binary no longer contains the old stub markers `candle backend is a stub until runtime wiring lands` or `TODO: wire Candle inference backend into harbor-model-api`
    - packaged binary still contains the runtime Candle note `candle candidate backend on side lane`, which is expected for the frozen promotion policy
- Installed the formal runtime-wired bundle on HarborOS `.182`:
  - streamed `/home/harbor-innovations/artifacts/harborbeacon-release-bundles/harbor-release-20260424-candle-runtime-r2.tar.gz` to `.182`
  - verified uploaded tarball sha256 matched `d0093ebae58d4d19b69e808675ccb600bead4cca0b5cb2b21b2e0b9d757afede`
  - ran `install_harboros_release.sh` successfully with:
    - install root: `/var/lib/harborbeacon-agent-ci`
    - writable root: `/mnt/software/harborbeacon-agent-ci`
    - current release symlink updated to `20260424-candle-runtime-r2`
  - core services and `harborgate-weixin-runner.service` restarted successfully
- Executed the packaged-binary `.182` Candle cutover rehearsal with explicit rollback:
  - launched the packaged `current/bin/harbor-model-api` directly on `4186` with `--backend candle`
  - verified `GET /healthz` on `4186` returned `backend.kind=candle`, `ready=true`, and `embedding dims 768`
  - verified direct `POST /v1/chat/completions` on `4186` returned the expected short Chinese plant-class answer `植株`
  - recorded a fresh baseline on the live `4175/4176` path before cutover:
    - `4176` stayed `openai_proxy` with `ready=true`
    - capability summary / friendly unsupported / explicit retrieval / natural-language retrieval / snapshot all stayed green
    - today’s live acceptance corpus under `/mnt/software/harborbeacon-agent-ci/acceptance-corpus` is `acceptance-note.md` plus `acceptance-sample.jpg/.json`, so the repeatable retrieval probes used `query=验收样例`
  - cutover path that passed:
    - stopped `harbor-model-api.service`
    - launched packaged `current/bin/harbor-model-api` on `4176` with `--backend candle`
    - verified `GET /healthz` on `4176` returned `backend.kind=candle` and `ready=true`
    - verified direct `POST /v1/chat/completions` on `4176` returned `植株`
    - verified direct `POST /v1/embeddings` on `4176` returned stable `768`-dim vectors
    - re-ran the HarborBeacon task-path matrix on `4175` and kept capability / unsupported / explicit retrieval / NL retrieval / snapshot green
  - explicit rollback that passed:
    - killed the temporary `4176` Candle pid `20108`
    - restarted `harbor-model-api.service` and confirmed `GET /healthz` on `4176` returned `backend.kind=openai_proxy` and `ready=true`
    - re-created the packaged Candle side lane on `4186` and confirmed it returned to `ready=true` with pid `22452`
    - re-ran the HarborBeacon task-path matrix and kept it green after rollback

### Architect Closeout

- Today’s runtime conclusion is now stronger than yesterday:
  - Candle cutover rehearsal on `.182` is `passed with rollback proven`
  - `4176 -> openai_proxy` remains the live default after rehearsal
  - the frozen HarborBeacon task and IM seams did not widen during the rehearsal
  - AIoT camera checks are now also back in the green, so camera/device ownership remains unchanged and unblocked
- The packaged-binary promotion lane is now complete for rehearsal:
  - the formal runtime-wired Candle release bundle was rebuilt on `.197`
  - the bundle was installed successfully on `.182`
  - the packaged `current/bin/harbor-model-api` passed the same cutover rehearsal that previously required the experiment binary override
- The newly exposed live packaging truth is now closed:
  - the earlier `.182` runtime gap between the release bundle and the experiment binary has been removed
  - the current HarborOS release version on `.182` is `20260424-candle-runtime-r2`
  - `4176` is back on the safe `openai_proxy` default, while `4186` remains the healthy packaged Candle side lane
- Next execution thread should be:
  - prepare the final controlled `.182 -> packaged Candle default` cutover wave
  - keep `4176=openai_proxy` as the live default until that explicit go/no-go decision is made
  - keep IM evidence collection scoped to the single live blocker category `weixin_dns_resolution` until platform reachability is restored

### Late Closeout - Candle Default Accepted

- A later same-day controlled closeout wave was completed on HarborOS `.182` without reopening any frozen northbound or IM seams.
  - current live default on `4176` is now the packaged `candle` backend
  - `4186` remains intentionally off after the cutover and short closeout wave
- Short observation replacement passed from the live `4176=candle` state:
  - three `GET http://127.0.0.1:4176/healthz` samples spaced by two minutes each all stayed `backend.kind=candle` and `ready=true`
  - the live env on `.182` still reports `HARBOR_MODEL_API_BACKEND=candle`
- Direct model probes on the live `4176` path stayed green:
  - direct `POST /v1/chat/completions` returned the short Chinese plant-class answer `植物`
  - direct `POST /v1/embeddings` returned stable `768`-dim vectors
- HarborBeacon task-path matrix stayed green on the live `4175 -> 4176` path after the default switch:
  - capability summary passed with `reply_pack.kind=capability_summary`
  - friendly unsupported passed with `reply_pack.kind=unsupported` and no raw endpoint leak
  - explicit retrieval for `query=验收样例` passed with grounded document + image citations and aligned text/image artifacts
  - natural-language retrieval for `帮我找到验收样例图片` passed with the same citation/artifact shape
  - snapshot passed through `camera_hub_service` and archived `/mnt/software/harborbeacon-agent-ci/camera-archive/cam-rtsp-192-168-3-231-1776967356663.jpg`
- Boundary guards remained green during the closeout:
  - `GET /api/gateway/status` stayed redacted under frozen `X-Contract-Version: 1.5`
  - Weixin still shows the same external blocker only: `transport.status=error`, `last_error=<urlopen error [Errno -2] Name or service not known>`
  - gateway delivery observability remained empty on this lane: `record_count=0`, `route_mode_counts={proactive:0, source_bound:0, unknown:0}`
  - HarborOS snapshot proxy still returned `HTTP 200` with `image/jpeg`
- Final architect acceptance for today:
  - `accepted: 4176=candle, 4186=off`
  - no blocker was introduced by the shortened closeout wave
  - future work should move to live observation and the isolated IM `weixin_dns_resolution` cleanup thread rather than reopening this cutover
