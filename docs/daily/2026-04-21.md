# 2026-04-21 Release-v1 Architect Closeout

## Status

- Verdict: `pending_live_weixin_dm_and_linux_bundle_build`
- Release/install baseline: `192.168.3.169`
- Live camera validation host: `192.168.3.182`
- Scope discipline: preserved

## What Closed Today

- HarborOS packaging/install lane is now in-repo and contract-tested.
  - added Linux-only builder bootstrap/build scripts, split-root install/rollback scripts, release templates, and the operator runbook
  - packaging shape is now locked by contract coverage instead of tribal memory
- TP-Link/Tapo RTSP path continuity is closed in code.
  - scan, manual connect, and password retry now keep discovered RTSP candidates such as `/stream1` and `/stream2`
  - HarborBeacon no longer drops back to stale generic defaults after discovery already found a device-specific path
- HarborOS local camera manual-add approval UX is closed in code.
  - local `owner/admin` manual add now goes straight to `hub.manual_add`
  - `operator` still routes through the `camera.connect` approval queue, so the approval boundary stays intact
- Real camera environment and credentials were validated live.
  - HarborOS `192.168.3.182` discovered and connected camera `192.168.3.231`
  - device `cam-rtsp-192-168-3-231` came online on `rtsp://admin:***@192.168.3.231:554/stream1`
  - snapshot succeeded and archived to `/mnt/software/harborbeacon-agent-ci/camera-archive/cam-rtsp-192-168-3-231-1776785451701.jpg`
- Frozen seams remain unchanged.
  - `POST /api/tasks`
  - `POST /api/notifications/deliveries`
  - `message`
  - `source.route_key`
  - `resume_token`

## Validation

- Ran: `python -m pytest tests/contracts/test_release_packaging_install_lane.py`
  - Result: `5 passed`
- Ran: `python -m pytest tests/contracts/test_aiot_boundary_guard.py`
  - Result: `1 passed`
- Ran: `python -m pytest tests/contracts/test_contract_docs.py`
  - Result: `8 passed`
- Ran: `cargo test owner_and_admin_manual_add_skip_camera_connect_approval_queue --bin agent-hub-admin-api`
  - Result: `1 passed`
- Ran: `cargo test operator_manual_add_still_routes_into_camera_connect_approval_queue --bin agent-hub-admin-api`
  - Result: `1 passed`
- Ran: `cargo test effective_rtsp_path_candidates_keep_existing_entries_and_add_tapo_fallbacks --lib`
  - Result: `1 passed`
- Ran: `cargo test preferred_rtsp_paths_prioritize_tapo_vendor_presets --lib`
  - Result: `1 passed`
- Live proof on `192.168.3.182`:
  - `POST /api/discovery/scan` found the TP-Link/Tapo camera at `192.168.3.231`
  - `POST /api/devices/manual` succeeded after the existing approval/replay path was exercised
  - `GET /api/state` showed `cam-rtsp-192-168-3-231` online
  - `GET /api/cameras/cam-rtsp-192-168-3-231/snapshot.jpg` returned a real image
- Important scope note:
  - the live `.182` run proved camera connectivity and snapshot truthfulness
  - the new local `owner/admin` no-approval behavior is source-tested here but still needs a fresh deploy before any host will show the smoother UX

## Remaining Gates

- One fresh private Weixin DM is still required on `.169`.
  - this is still the missing live proof for real HarborGate ingress, source-bound reply from `POST /api/tasks`, and one `needs_input -> resume` roundtrip
- A real Linux release bundle was not built today.
  - `tools/build_release_bundle.sh` is Linux-only
  - this Windows host has no WSL available
  - SSH access to the Debian builder `192.168.3.223` is still blocked by `Permission denied`
  - today's closeout therefore ships code, docs, tests, and builder instructions, but not a produced `harbor-release-*.tar.gz`

## Release View

- Safe to merge/push today's code, docs, and tests: yes
- Safe to claim a release bundle already exists: no
- Highest current risk: environment and ops access, not a reopened framework, IM, HarborOS, or AIoT boundary gap

## Next Step

1. Obtain working SSH access to Debian builder `192.168.3.223` and run `tools/build_release_bundle.sh`.
2. Deploy the resulting bundle to `.169` or `.182` and recheck the local `owner/admin` manual-add no-approval path live.
3. Send one fresh private Weixin DM on `.169` to close the last IM parity proof.
