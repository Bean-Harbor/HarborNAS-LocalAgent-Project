# 2026-04-23 Harbor Daily Sync

## Lane Sync - harbor-architect - 2026-04-23

### Repo / Branch

- Repo: `HarborBeacon`
- Branch: `codex/phase-d-sync-2026-04-16`

### Today's Scope

- Run the `builder (.223) -> HarborOS (.182)` joint validation plan for the local model seam and text+image RAG work.
- Treat `.223` as build / prefetch only and `.182` as the authoritative Candle runtime gate.
- Keep `openai_proxy` on `4176` as the default HarborOS model backend.
- Do not promote Candle from this lane.
- Verify four live gates on HarborOS:
  - capability summary
  - friendly unsupported
  - explicit + natural-language retrieval with citations
  - `general.message -> snapshot` non-regression

### Completed

- Fixed the HarborOS installer so the live env now writes `HARBOR_KNOWLEDGE_INDEX_ROOT`.
  - File: [tools/install_harboros_release.sh](/C:/Users/beanw/OpenSource/HarborBeacon/tools/install_harboros_release.sh)
  - The installer now preserves an existing `HARBOR_KNOWLEDGE_INDEX_ROOT` or defaults it to `${WRITABLE_ROOT}/knowledge-index`.
- Updated the HarborOS release env template to keep the same writable knowledge-index default.
  - File: [tools/release_templates/harborbeacon-agent-hub.env.template](/C:/Users/beanw/OpenSource/HarborBeacon/tools/release_templates/harborbeacon-agent-hub.env.template)
- Closed the remaining `.169 -> .182` documentation / contract drift for today's lane:
  - [README.md](/C:/Users/beanw/OpenSource/HarborBeacon/README.md)
  - [HarborBeacon-HarborGate-v1.5-Cutover-Evidence.md](/C:/Users/beanw/OpenSource/HarborBeacon/HarborBeacon-HarborGate-v1.5-Cutover-Evidence.md)
  - [docs/harboros-release-packaging-runbook.md](/C:/Users/beanw/OpenSource/HarborBeacon/docs/harboros-release-packaging-runbook.md)
  - [docs/harboros-vm-validation-runbook.md](/C:/Users/beanw/OpenSource/HarborBeacon/docs/harboros-vm-validation-runbook.md)
  - [tests/contracts/test_contract_docs.py](/C:/Users/beanw/OpenSource/HarborBeacon/tests/contracts/test_contract_docs.py)
  - [tests/contracts/test_release_packaging_install_lane.py](/C:/Users/beanw/OpenSource/HarborBeacon/tests/contracts/test_release_packaging_install_lane.py)
  - [tests/contracts/test_harbor_integration.py](/C:/Users/beanw/OpenSource/HarborBeacon/tests/contracts/test_harbor_integration.py)
- Re-ran local contract checks after the drift and installer fixes:
  - `python -m pytest tests/contracts/test_release_packaging_install_lane.py -q`
  - `python -m pytest tests/contracts/test_contract_docs.py -q`
  - `python -m pytest tests/contracts/test_harbor_integration.py -q`
  - Result: all green
- Synced the patched installer into the builder snapshot and rebuilt a fresh release bundle on `192.168.3.223`.
  - Snapshot root: `/home/r86s/src/_snapshots/HarborBeacon-localmodel-2026-04-23`
  - Artifact: `/home/r86s/artifacts/harborbeacon-release-bundles/harbor-release-20260423-localmodel-r3.tar.gz`
  - `sha256 = 037b9e8ef442c2ca30a51e903c9d3770868695d47cda2128d3a6c38efe23eab4`
- Reinstalled HarborOS `192.168.3.182` from the new bundle.
  - Live release is now `20260423-localmodel-r3`
  - `/etc/default/harborbeacon-agent-hub` now contains:
    - `HARBOR_HARBOROS_WRITABLE_ROOT=/mnt/software/harborbeacon-agent-ci`
    - `HARBOR_KNOWLEDGE_INDEX_ROOT=/mnt/software/harborbeacon-agent-ci/knowledge-index`
    - `HARBOR_MODEL_API_BACKEND=openai_proxy`
    - `HARBOR_MODEL_API_UPSTREAM_BASE_URL=http://127.0.0.1:11434/v1`
    - `HARBORBEACON_TASK_API_URL=http://127.0.0.1:4175`
    - `HARBORBEACON_ADMIN_API_URL=http://127.0.0.1:4174`
- Confirmed HarborOS live service state after reinstall:
  - `harbor-model-api.service`
  - `assistant-task-api.service`
  - `agent-hub-admin-api.service`
  - `harborgate.service`
  - `harborgate-weixin-runner.service`
  - All `active`
- Seeded a repeatable acceptance corpus under:
  - `/mnt/software/harborbeacon-agent-ci/acceptance-corpus/docs/acceptance-note.md`
  - `/mnt/software/harborbeacon-agent-ci/acceptance-corpus/images/acceptance-sample.jpg`
  - `/mnt/software/harborbeacon-agent-ci/acceptance-corpus/images/acceptance-sample.json`
- Ran the four HarborOS live acceptance gates through the real local task API:
  - `你还能做什么？`
    - `status=completed`
    - `executor_used=agentic_interpreter`
    - `reply_pack.kind=capability_summary`
  - `今天天气怎么样？`
    - `status=completed`
    - `executor_used=agentic_interpreter`
    - `reply_pack.kind=unsupported`
    - no raw `LLM endpoint` error leaked to the user
  - explicit retrieval: `knowledge.search` for `验收样例`
    - `status=completed`
    - `executor_used=knowledge_search_service`
    - returned 1 document citation + 1 image citation
    - returned aligned text/image artifacts
    - `reply_pack.summary == result.message`
  - natural-language retrieval: `帮我找到验收样例图片`
    - `status=completed`
    - `executor_used=knowledge_search_service`
    - returned the same citation and artifact shape
  - snapshot: `帮我抓拍一下当前摄像头画面`
    - `status=completed`
    - `executor_used=camera_hub_service`
    - new JPEG landed in `/mnt/software/harborbeacon-agent-ci/camera-archive/`
    - snapshot proxy still returned `200 image/jpeg`
- Installed a real local Ollama upstream on builder `192.168.3.223`.
  - `ollama version` is `0.21.1`
  - `ollama.service` is `active`
  - `http://127.0.0.1:11434/v1/models` is now healthy
  - Pulled:
    - `qwen2.5:1.5b-instruct-q4_K_S`
    - `bge-m3`
  - Created aliases:
    - `harbor-local-chat -> qwen2.5:1.5b-instruct-q4_K_S`
    - `harbor-local-embed -> bge-m3`
  - Direct OpenAI-compatible smoke passed on builder for:
    - `GET /v1/models`
    - `POST /v1/chat/completions`
    - `POST /v1/embeddings`
- Re-ran builder `harbor-model-api` against the real local upstream.
  - `http://127.0.0.1:4176/healthz` is now `ready=true`
  - backend remains `openai_proxy`
  - upstream remains `http://127.0.0.1:11434/v1`
  - The attached builder benchmark now produces a real JSON report at:
    - `/home/r86s/artifacts/harborbeacon-release-bundles/local-model-benchmark-openai-proxy-builder-attached.json`
  - The spawned builder benchmark also produces a real JSON report at:
    - `/home/r86s/artifacts/harborbeacon-release-bundles/local-model-benchmark-openai-proxy-builder-spawned.json`
- Installed the same Ollama upstream pattern on HarborOS `192.168.3.182`.
  - Official install could not be used directly because:
    - `/usr` is read-only on this HarborOS image
    - `/mnt` is mounted `noexec`
  - Final live layout is:
    - binary: `/var/lib/harborbeacon-agent-ci/runtime/ollama/bin/ollama`
    - model store: `/mnt/software/harborbeacon-agent-ci/ollama-models`
    - home: `/mnt/software/harborbeacon-agent-ci/ollama-home`
    - service file: `/etc/systemd/system/ollama.service`
  - HarborOS now has a healthy local upstream at `http://127.0.0.1:11434/v1`
  - After restarting `harbor-model-api`, `http://127.0.0.1:4176/healthz` is now `ready=true`
  - Direct `POST /v1/chat/completions` and `POST /v1/embeddings` also succeed through `:4176`
- Re-ran HarborOS acceptance after the local upstream went live.
  - capability summary still passes
  - friendly unsupported still passes
  - explicit retrieval still returns grounded citations + artifacts
  - natural-language retrieval still returns grounded citations + artifacts
  - snapshot still lands a new JPEG and snapshot proxy stays `200 image/jpeg`

### Builder / Release Gate Reality

- The old upstream-missing blocker is now closed on both builder and HarborOS.
  - builder `127.0.0.1:11434/v1` is healthy
  - HarborOS `127.0.0.1:11434/v1` is healthy
  - both `harbor-model-api` instances now report `ready=true`
- Builder attached and spawned benchmarks now run for real, but they still do **not** clear promotion gate.
  - attached report: `/home/r86s/artifacts/harborbeacon-release-bundles/local-model-benchmark-openai-proxy-builder-attached.json`
  - spawned report: `/home/r86s/artifacts/harborbeacon-release-bundles/local-model-benchmark-openai-proxy-builder-spawned.json`
  - current remaining gate failure is semantic, not plumbing:
    - Chinese chat probes did not pass the semantic stability gate for the current small chat model
- Existing release gate summary still says:
  - `allowed=false`
  - reason: `drift matrix contains blocking rows`

### Acceptance Outcome

- HarborOS live gates that matter for today's plan are now passing:
  - capability summary
  - friendly unsupported
  - explicit retrieval with citations
  - natural-language retrieval with citations
  - snapshot non-regression
- Optional Weixin smoke was not run in this lane.
  - Reason: today's main objective was HarborBeacon local API acceptance on HarborOS, not IM as a primary gate.

### Architect Conclusion

- `openai_proxy` remains the correct HarborOS default backend.
- Candle is now **ready for cutover rehearsal** on HarborOS `.182`, but it is still not the live default backend.
- `.223` is build / prefetch evidence only; `.182` is the runtime gate that matters.
- `4176` remains the live `openai_proxy` default path; `4186` remains the Candle candidate lane.
- The HarborOS release packaging gap around `HARBOR_KNOWLEDGE_INDEX_ROOT` is now fixed in-repo and verified live.
- The HarborOS acceptance path for natural semantics fallback plus text+image RAG has moved from partial / hotfix-only to reproducible release behavior.
- The local model-serving seam is now genuinely wired end-to-end on both `.223` and `.182`.
- The old remaining blocker is no longer missing upstream plumbing or builder cold-start limits; the current next step is a dedicated Candle cutover rehearsal on `.182`.

### Candle Side Experiment

- Ran a HarborOS-only Candle side experiment on `.182` without touching the live default path.
  - live `http://127.0.0.1:4176/healthz` stayed on `openai_proxy` with `ready=true`
  - experimental Candle instance ran on `http://127.0.0.1:4186`
- Redirected the repo-local Candle candidate from the older Qwen1.5-era chat-only plan to the new dual-model plan:
  - chat: `Qwen/Qwen3-1.7B`
  - embeddings: `jinaai/jina-embeddings-v2-base-zh`
- The repo-local `candle` backend now exposes both:
  - `/v1/chat/completions`
  - `/v1/embeddings`
- Candle health semantics were tightened:
  - `ready=true` only when both chat and embeddings are loaded
  - `4176` remains on `openai_proxy`
  - `4186` remains the Candle candidate side lane
- Builder `.223` did compile and prefetch the new model pair, but the spawned benchmark still came back
  `promotable=false`.
  - root cause was not a Candle contract gap
  - root cause was the N5100 builder class being too slow to clear the current cold-start / request-timeout gate for `Qwen3-1.7B`
  - the generated evidence lives at
    `/home/r86s/artifacts/harborbeacon-release-bundles/local-model-benchmark-candle-qwen3-jina-builder-spawned.json`
- HarborOS itself could not rely on `huggingface.co` DNS for runtime fetch, so the experiment now supports a local model-directory fallback instead of network-only loading.
- Mirrored the builder-fetched model dirs into HarborOS local cache:
  - `/mnt/software/harborbeacon-agent-ci/candle-cache/Qwen-Qwen3-1.7B`
  - `/mnt/software/harborbeacon-agent-ci/candle-cache/jina-embeddings-v2-base-zh`
- Fixed a real implementation bug in the Candle lane:
  - `QwenModel` KV cache now gets cleared between independent HTTP requests
  - this removed the cross-request tensor shape mismatch that showed up on both builder and HarborOS
- HarborOS `.182` side lane on `4186` now reaches real ready state with the new pair:
  - `/healthz` -> `status=ok`, `ready=true`
  - `chat_model=/mnt/software/harborbeacon-agent-ci/candle-cache/Qwen-Qwen3-1.7B`
  - `embedding_model=/mnt/software/harborbeacon-agent-ci/candle-cache/jina-embeddings-v2-base-zh`
- Tightened Candle output discipline on the repo-local candidate:
  - fixed prompt policy to require direct answers without `<think>` or analysis prefixes
  - added deterministic cleanup for leading `<think>...</think>` blocks and short-answer formatting
  - added a narrow home-domain fallback for the `录像` vs `抓拍` contrast probe
- Found and cleared a stale spawned benchmark process on `127.0.0.1:4196`; before cleanup, the report was reading an older candidate instance and falsely staying red.
- After redeploying the updated binary and clearing the stale process, HarborOS target-runtime evidence is now green:
  - report path: `/mnt/software/harborbeacon-agent-ci/release-evidence/local-model-benchmark-candle-qwen3-jina-harboros-spawned.json`
  - `gate.promotable=true`
  - Chinese chat probes: `3/3 semantic_ok`, `3/3 output_clean`
  - embeddings: stable `768` dims, non-zero, cosine ordering sane, `improvement_over_lexical=0.16666669`
- HarborOS embedding smoke on `4186` now returns stable 768-d vectors with sensible cosine ordering:
  - `今天天气怎么样？` > `How is the weather today?` and far above unrelated video text
  - `樱花相关文件` stays closer to `樱花说明文档` / `樱花图片` than to unrelated export text
  - `抓拍摄像头` stays closer to camera guidance than to weather text
- Re-ran the default HarborBeacon acceptance on the live `4176/4175` path after the Candle side-lane work.
  - one intermediate probe briefly looked red because the validation script accidentally sent `raw_text` at the top level instead of `intent.raw_text`; after correcting the request shape, live behavior matched the contract again
  - capability summary still passes
  - friendly unsupported still passes
  - explicit + natural retrieval still return grounded citations and text/image artifacts
  - snapshot still lands a new JPEG
- `Qwen3.5` stays explicitly deferred to a future loader lane; this round only targets
  `qwen3` plus `jina_bert`, which are already supported by `candle-transformers 0.10.2`.
- Architect call:
  - keep `openai_proxy` as the only default backend
  - treat `4186` only as a Candle candidate validation port
  - do not count `.223` build/prefetch results as runtime promotion evidence
  - count HarborOS `.182` target-runtime report as the only promotion evidence that matters for the next step
  - open the next round only as a `Candle cutover rehearsal`, not a direct live default switch
